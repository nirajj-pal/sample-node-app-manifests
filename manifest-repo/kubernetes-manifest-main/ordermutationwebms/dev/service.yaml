apiVersion: v1
kind: Service
metadata:
  name: ordermutationwebms
  namespace: dev
spec:
  selector:
    app: ordermutationwebms
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP