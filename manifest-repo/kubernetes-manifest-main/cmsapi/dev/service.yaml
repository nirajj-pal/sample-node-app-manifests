apiVersion: v1
kind: Service
metadata:
  name: cmsapi
  namespace: prod
spec:
  selector:
    app: cmsapi
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
