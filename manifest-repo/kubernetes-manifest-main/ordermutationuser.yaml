apiVersion: v1
kind: Service
metadata:
  name: order-mutation-user
  namespace: poc
spec:
  selector:
    app: order-mutation-user
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 32524
  type: NodePort