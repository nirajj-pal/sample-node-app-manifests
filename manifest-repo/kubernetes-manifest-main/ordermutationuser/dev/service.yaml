apiVersion: v1
kind: Service
metadata:
  name: ordermutationuser
  namespace: dev
spec:
  selector:
    app: ordermutationuser
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP