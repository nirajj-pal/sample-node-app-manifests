apiVersion: v1
kind: Service
metadata:
  name: sample-node-app
  labels:
    app: sample-node-app
spec:
  selector:
    app: sample-node-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
